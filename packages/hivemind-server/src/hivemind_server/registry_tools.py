"""Artifact + tool-registry MCP tools. Filled in during the artifact-store and tool-registry
milestones. `attach` is a no-op stub until then so the server boots with the graph/schema/guide
surface alone."""
from __future__ import annotations


def attach(mcp, project) -> None:  # pragma: no cover - replaced in later milestones
    return None
