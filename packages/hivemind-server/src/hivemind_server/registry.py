"""Tool registry: publish/resolve/yank standalone tools. Filled in the tool-registry milestone.
attach_tools is a stub until then."""
from __future__ import annotations


def attach_tools(mcp, project, envelope, RO, WRITE, base) -> None:  # pragma: no cover
    return None
