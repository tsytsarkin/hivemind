{
  "description": "Re-injects this session's Hivemind project pin on startup, clear, compact, resume and fork, so a rebuilt context never leaves the project choice behind.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact|resume|fork",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
